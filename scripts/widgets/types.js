// TODO Instead of defining all widgets implicitly here, we should try to 
// have all types of widgets independently defined and auto-discovered. The
// auto-discovery could be made with a dynamically written require() that can
// be written by the server-side application as explained here:
// http://stackoverflow.com/a/5893104/167333

define({
  'gamepad': {
    id: '1'
  }
});
